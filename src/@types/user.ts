export interface User {
  id: number;
  avatar: string;// Omit
  code: string;// 
  eduMail: string;
  isActive: boolean;
  name: string;
  personalMail: string;
  phoneNumber: string;
  role: number;
  schoolYear: string;
  curriculumId: number;
}
